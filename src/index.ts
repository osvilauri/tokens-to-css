/**
 * tokens-to-css — the public surface.
 *
 * Story 1.1 establishes the package; the Main Entry, the error type and the
 * failure codes arrive in Story 1.2, and the conversion behind them in 1.10.
 * Nothing else in `src/` is ever exported from here (AD-14).
 */

/** The package version this build was cut from. */
export const VERSION = '0.0.0'
